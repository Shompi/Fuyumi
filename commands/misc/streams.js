const { Command } = require('discord-akairo');

class GoLiveConfig extends Command {
	constructor() {
		super('golive', {
			aliases: ['golive', 'glive'],
			args: [
				{

				}
			]
		});
	}

	async exec(message, args) {

	}
}